// Initial state
import { INITIAL_STATE } from '../../../common/app-const';

const auth = (state = INITIAL_STATE.auth, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default auth;
